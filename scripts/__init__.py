"""Helper package regrouping all workflow steps.

The historical automation expected every module to live under ``scripts`` so we
expose the relocated modules here.  Keeping this file explicit also makes the
package compatible with ``python -m scripts.<module>`` invocations.
"""

__all__ = []
